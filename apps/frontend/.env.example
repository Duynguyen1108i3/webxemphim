# Default: /api is proxied by Vite in development and Nginx in containers.
VITE_API_URL=/api
# Only set a full HTTPS API origin when frontend and backend are intentionally deployed separately.
# VITE_API_URL=https://api.example.com/api
