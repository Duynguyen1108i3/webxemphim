[build]
  command = "npm run build --workspace @streamforge/utils && npm run build --workspace @streamforge/frontend"
  publish = "apps/frontend/dist"

[build.environment]
  NODE_VERSION = "22.14.0"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
